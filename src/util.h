#pragma once

#include <string>
#include <vector>

using std::string;
using std::vector;

// Show some basic statistics about a collection of floats.
void Look(const string& title, const vector<float>& ff);
